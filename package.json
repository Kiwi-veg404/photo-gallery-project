{
  "name": "photo-gallery-project",
  "version": "1.0.0",
  "description": "Photo gallery project with likes/dislikes and local storage",
  "main": "index.html",
  "type": "module",
  "scripts": {
    "start": "web-dev-server --node-resolve --open --watch",
    "build": "rimraf dist && rollup -c rollup.config.js",
    "test": "echo \"No tests specified yet\""
  },
  "keywords": ["webcomponents", "lit", "photo-gallery"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "lit": "^3.1.0"
  },
  "devDependencies": {
    "@web/dev-server": "^0.4.0",
    "@rollup/plugin-node-resolve": "^15.0.0",
    "@rollup/plugin-terser": "^0.4.0",
    "rollup": "^4.0.0",
    "rimraf": "^5.0.0"
  }
}
